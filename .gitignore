# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/

# Local state + generated output (machine-specific, not source)
data/
output/
*.db
*.db-wal
*.db-shm
*.sqlite
